spring.config.import=optional:dotenv:.env
spring.application.name=${SPRING_APPLICATION_NAME:pet_link}

server.port=${SERVER_PORT:8080}

# Banco de Dados PostgreSQL
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/petlink?sslmode=disable}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:petlink}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:petlink}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}

#spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Pool de conex�es
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.connection-init-sql=${SPRING_DATASOURCE_HIKARI_CONNECTION_INIT_SQL:CREATE EXTENSION IF NOT EXISTS postgis;}


# Actuator (monitoramento)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized

# Logs
logging.level.org.springframework.web=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=WARN

# Configura��es de upload (opcional)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Timezone
spring.jackson.time-zone=America/Sao_Paulo

jwt.expiration=${JWT_EXPIRATION:900000}
jwt.secret=${JWT_SECRET}

logging.pattern.console = %d{yyyy-MM-dd HH:mm} == %logger{40} >>> %msg%n